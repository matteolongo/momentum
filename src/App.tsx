export default function App() {
  return (
    <main className="app-shell">
      <p className="eyebrow">Local-only PWA</p>
      <h1>Momentum</h1>
      <p className="lede">Habit tracking with a game loop, not a dashboard pile.</p>
    </main>
  );
}
